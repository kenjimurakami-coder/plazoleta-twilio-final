server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/usuarios_final_plazoleta
    username: postgres
    password: Induccion2025+

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

appDescription: "Pragma Powerup"
appVersion: "1.0.0"

TWILIO_ACCOUNT_SID: ${TWILIO_ACCOUNT_SID}
TWILIO_AUTH_TOKEN: ${TWILIO_AUTH_TOKEN}
TWILIO_FROM_PHONE_NUMBER: ${TWILIO_FROM_PHONE_NUMBER}
